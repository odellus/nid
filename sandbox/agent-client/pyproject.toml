[project]
name = "agent-client"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "agent-client-protocol>=0.8.1",
    "ipython>=9.10.0",
    "websockets>=16.0",
]
